{
  "name": "hacktracker",
  "version": "1.0.0",
  "private": true,
  "description": "Discover Hackathons. Track Achievements. Build Your Legacy.",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "seed": "npm --prefix server run prisma:seed",
    "migrate": "npm --prefix server run prisma:migrate",
    "postinstall": "npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
